fixtures:
    symlinks:
      pe_metrics_dashboard: "#{source_dir}"
    repositories:
      stdlib:
        repo: https://github.com/puppetlabs/puppetlabs-stdlib.git
        ref: 4.3.2
      apt:
        repo: https://github.com/puppetlabs/puppetlabs-apt.git
        ref: 4.3.0
      grafana: 
        repo: https://github.com/voxpupuli/puppet-grafana.git
        ref: 4.0.2
    forge_modules:
